Claude: LLM (Large Language Model)

Steps:
1) claude code install
   get cmd from google and run it in cmd
   or
   npm install -g @anthropic-ai/claude-code

2)git bash

3) claude in cmd it opens a claude

4) choose plan 

commands:
/statusline -> info
/clear --> context and history will clear
/status 

Way of using claude:
    1) terminal
    2) IDE
    why we need IDE?
        1) File system access
        2) code editing
        3) Integrated terminal
        4) Side by side view
        5) File management

In IDE:
    install claude code for VS code extension

CLAUDE.md:
    very important
    coz: it will remember everything and will run every time